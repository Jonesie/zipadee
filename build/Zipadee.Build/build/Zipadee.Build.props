<Project>

  <PropertyGroup>
    <!-- Nothing actually compiles for TargetFramework, so a bin\Debug\net8.0\ subfolder is
         just confusing - drop the framework-named segment from the output path. -->
    <AppendTargetFrameworkToOutputPath Condition="'$(AppendTargetFrameworkToOutputPath)' == ''">false</AppendTargetFrameworkToOutputPath>
  </PropertyGroup>

  <!-- The PropertyPageSchema items that surface the archive settings in Visual Studio's UI are
       deliberately NOT here - they're in Zipadee.Build.targets, because they have to be
       evaluated after Microsoft's own rules. See the comment there. -->

</Project>
